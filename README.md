# Chess Game

A full chess implementation -- Python CLI and a JS web app -- playable
against an AI with four difficulty levels (facile, moyen, difficile,
impossible). No external dependencies: the move generator, rules engine
and AI are all written from scratch.

Work in progress -- see commit history for the build-up.
